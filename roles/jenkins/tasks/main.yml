- name: Create
  community.general.jenkins.job:
    config: "{{ lookup('file', '/tmp/job.xml') }}"

    name: test
    url: http://172.31.25.13:8080
    user: admin
    password: "{{jenkins_password}}"
  loop:
    - frontend
    - cart
    - catalogue

- name: Create Config file
  ansible.builtinn.template:
    src: job.xml
    dest: /tmp/job.xml